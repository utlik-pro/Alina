# Crystal v2 — отдельная версия, 5 сентября 2026

Исходная рабочая папка: `/Users/admin/Alina` (ветка `develop`).
Разработка v2: `/Users/admin/Alina/.worktrees/crystal-v2` (ветка `codex/crystal-v2`).

## Сохранённая точка

Базовый коммит v2: `a2161e3` (`chore: preserve reviewed baseline before Crystal v2`).
Он включает локальные исправления, сделанные при аудите до начала v2.
Архив исходников: `/Users/admin/Alina/.version-backups/2026-09-05-before-v2/working-source.tar.gz`.
Рядом находятся SHA256-манифест, исходный commit и patch локальных изменений.
Архив исходников не является резервной копией production-базы, секретов или всех личных неотслеживаемых файлов.

## Как вернуться

Пока v2 не опубликована, возврат не требует никаких действий: исходная папка и текущий production остаются прежними. Для работы со старым кодом откройте `/Users/admin/Alina`.

Для отдельного чистого checkout сохранённой точки из репозитория:

```sh
git worktree add --detach /Users/admin/Alina/.worktrees/crystal-baseline a2161e3
```

Не выполняйте `reset --hard` или распаковку архива поверх текущих файлов: это может затереть более позднюю работу. При необходимости архив сначала распаковывают в пустую папку и сравнивают файлы.

Если v2 позже будет опубликована, возврат сервиса следует делать на заранее зафиксированную production-ревизию в системе развёртывания. `a2161e3` — локальная исходная точка, а не подтверждённая ревизия production.

## Первый набор изменений

- Instagram отправляет ответы через свой канал без зависимости от наличия Wappi.
- Явный отказ или перенос решения («No thanks», «Don't book», «I will confirm later») завершает ход до вызова модели и инструментов бронирования.
- Смена массажа лица на массаж тела обновляет услугу и сбрасывает длительность предыдущей услуги, сохраняя контакт.
- Выбор массажа лица после рекламы баночного комбо снимает старый рекламный контекст, чтобы он не возвращал цену 275 AED.
- Реплеи проходят через обработчик канала с подменёнными внешними сервисами; включают отказ и последующее возвращение клиента.

## Границы проверки

В этой копии не подключались реальные каналы сообщений и production-база, не создавались реальные записи YClients и не выполнялся deploy. Локальные тесты не заменяют полный прогон на отдельном тестовом канале с актуальным расписанием.

Остаются следующие этапы из аудита: стойкая к перезапуску защита от дублей, сверка локальных записей с YClients, проверка конкурирующих автоответчиков и полный сценарий записи/переноса/отмены на тестовом окружении.
